# Build artifacts
dist/

# Node / Next.js (site/)
site/node_modules/
site/.next/
site/out/
site/next-env.d.ts
site/.vercel/
site/.env*.local
site/tsconfig.tsbuildinfo

# Archived source PDFs — kept LOCAL-ONLY (large binaries), never committed.
# The INDEX.md / _manifest.json under docs/sources/ stay tracked as references.
docs/sources/**/*.pdf

# Knowledge-graph artifacts (regenerable via /graphify)
graphify-out/

# Python bytecode
__pycache__/
